[project]
name = "ms-learn-agent"
version = "0.1.0"
description = "Agentic course advisor for the Microsoft Learn catalog (Claude tool use)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.100.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
